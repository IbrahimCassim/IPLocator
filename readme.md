### Config
Set up s3 bucket with counter.json with starting visitorCount at 1.

Make sure there is credentials.

### Getting Started
``` bash
# Build your client application first
cd client
npm install
npm run build

# Start the server
cd ../server
node index.js

# Open your browser and navigate to localhost:3000
```
